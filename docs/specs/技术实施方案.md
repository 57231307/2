# 纺织面料ERP系统 - 技术实施方案

> **版本**：v1.0  
> **日期**：2026-06-06  
> **状态**：已审核

## 目录

1. [技术方案概览](#1-技术方案概览)
2. [核心模块实施计划](#2-核心模块实施计划)
3. [数据库实现方案](#3-数据库实现方案)
4. [API 实现规范](#4-api-实现规范)
5. [事件驱动架构实现](#5-事件驱动架构实现)
6. [性能优化方案](#6-性能优化方案)
7. [安全实现方案](#7-安全实现方案)
8. [测试实现方案](#8-测试实现方案)

---

## 1. 技术方案概览

### 1.1 技术栈回顾

| 层级 | 技术选型 | 版本 |
|------|---------|------|
| 前端框架 | Angular | 22.x |
| 前端状态管理 | NgRx / Signal | 18.x |
| 前端 UI | Tailwind CSS | 3.x |
| 后端框架 | NestJS | 10.x |
| 数据库 ORM | TypeORM | 0.3.x |
| 数据库 | PostgreSQL | 15.x |
| 缓存 | Redis | 7.x |
| 认证 | JWT + Passport | - |
| 文档 | Swagger / OpenAPI | - |
| 容器化 | Docker + Docker Compose | - |

### 1.2 实施阶段划分

| 阶段 | 周数 | 主要工作 |
|------|------|---------|
| 阶段 1 | 第 9-10 周 | 基础数据管理模块 + 用户权限管理 |
| 阶段 2 | 第 11-12 周 | 销售管理 + 采购管理模块 |
| 阶段 3 | 第 13-14 周 | 库存管理 + 批次管理 |
| 阶段 4 | 第 15-16 周 | 颜色配方 + 花型管理 + 打样流程 |
| 阶段 5 | 第 17-18 周 | 财务成本核算 + 报表系统 |
| 阶段 6 | 第 19-20 周 | 系统集成 + 移动应用 + 测试上线 |

---

## 2. 核心模块实施计划

### 2.1 系统管理模块

#### 2.1.1 用户管理

**实现内容**：
- 用户 CRUD 操作
- 用户状态管理（激活/锁定/禁用）
- 用户角色分配
- 用户密码重置
- 用户操作日志记录

**技术要点**：
```typescript
// 用户服务示例
@Injectable()
export class UserService {
  constructor(
    @InjectRepository(User)
    private userRepository: Repository<User>,
  ) {}

  async create(createUserDto: CreateUserDto): Promise<User> {
    // 密码加密
    const hashedPassword = await bcrypt.hash(createUserDto.password, 10);
    
    const user = this.userRepository.create({
      ...createUserDto,
      password: hashedPassword,
    });
    
    return this.userRepository.save(user);
  }

  async findAll(page: number, limit: number): Promise<PaginatedResult<User>> {
    const [items, total] = await this.userRepository.findAndCount({
      relations: ['roles'],
      skip: (page - 1) * limit,
      take: limit,
    });
    
    return {
      items,
      total,
      page,
      limit,
    };
  }
}
```

**API 端点**：
- `GET /api/v1/system/users` - 获取用户列表
- `GET /api/v1/system/users/:id` - 获取用户详情
- `POST /api/v1/system/users` - 创建用户
- `PUT /api/v1/system/users/:id` - 更新用户
- `DELETE /api/v1/system/users/:id` - 删除用户
- `POST /api/v1/system/users/:id/reset-password` - 重置密码

#### 2.1.2 角色与权限管理

**实现内容**：
- 角色 CRUD 操作
- 权限管理（角色权限分配）
- 权限缓存（Redis）
- 动态权限检查

**权限层级设计**：
```
系统权限
├── 基础数据模块
│   ├── 查看
│   ├── 新增
│   ├── 编辑
│   └── 删除
├── 销售管理模块
│   ├── 订单查看
│   ├── 订单创建
│   ├── 订单审批
│   └── 订单导出
├── 库存管理模块
│   ├── 入库管理
│   ├── 出库管理
│   └── 库存盘点
└── ...
```

### 2.2 基础数据管理模块

#### 2.2.1 产品管理

**实体属性**：
- 产品编码（唯一）
- 产品名称
- 产品类型（面料/坯布/助剂等）
- 规格描述
- 单位（米/千克/卷等）
- 颜色配方（关联）
- 花型设计（关联）
- 标准成本
- 销售价格
- 状态（启用/禁用）
- 图片/附件

**业务规则**：
- 产品编码自动生成：`PRD-{YYYYMMDD}-{0000}`
- 产品删除时检查是否被订单、库存引用
- 产品价格变更需要记录日志

#### 2.2.2 客户管理

**实体属性**：
- 客户编码（唯一）
- 客户名称
- 客户类型（普通/VIP/战略）
- 联系人信息
- 联系电话
- 电子邮箱
- 地址信息
- 信用额度
- 账期
- 所属销售员
- 状态（启用/禁用）
- 备注

**业务规则**：
- 客户编码自动生成：`CUS-{YYYYMMDD}-{0000}`
- 客户信用额度检查（订单创建时验证）
- 客户分级管理

#### 2.2.3 供应商管理

类似客户管理，包含供应商评估、采购价目表等。

#### 2.2.4 仓库管理

**实体属性**：
- 仓库编码（唯一）
- 仓库名称
- 仓库类型（原料仓/成品仓/半成品仓/委外仓）
- 地址
- 负责人
- 库位配置（JSON）
- 状态

---

## 3. 数据库实现方案

### 3.1 数据库设计原则

1. **规范化**：遵循第三范式，减少数据冗余
2. **性能**：合理索引、分区表设计、查询优化
3. **可扩展性**：预留扩展字段、使用 JSONB 存储灵活数据
4. **数据完整性**：外键约束、唯一约束、检查约束
5. **审计**：所有表包含 created_at、updated_at、created_by、updated_by

### 3.2 核心表实现

#### 3.2.1 产品表（products）

```sql
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(50) UNIQUE NOT NULL,
  name VARCHAR(200) NOT NULL,
  type VARCHAR(50) NOT NULL,
  spec TEXT,
  unit VARCHAR(20) NOT NULL,
  color_formula_id UUID REFERENCES color_formulas(id),
  pattern_id UUID REFERENCES patterns(id),
  standard_cost DECIMAL(12, 4),
  sale_price DECIMAL(12, 4),
  status VARCHAR(20) DEFAULT 'ACTIVE',
  images JSONB,
  attachments JSONB,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_by UUID
);

CREATE INDEX idx_products_code ON products(code);
CREATE INDEX idx_products_type ON products(type);
CREATE INDEX idx_products_status ON products(status);
```

#### 3.2.2 客户表（customers）

```sql
CREATE TABLE customers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(50) UNIQUE NOT NULL,
  name VARCHAR(200) NOT NULL,
  type VARCHAR(50) DEFAULT 'NORMAL',
  contact_person VARCHAR(100),
  phone VARCHAR(20),
  email VARCHAR(100),
  address TEXT,
  credit_limit DECIMAL(12, 4) DEFAULT 0,
  payment_terms INTEGER DEFAULT 30,
  salesperson_id UUID REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'ACTIVE',
  metadata JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_by UUID
);

CREATE INDEX idx_customers_code ON customers(code);
CREATE INDEX idx_customers_salesperson ON customers(salesperson_id);
CREATE INDEX idx_customers_status ON customers(status);
```

#### 3.2.3 销售订单表（sales_orders）

```sql
CREATE TABLE sales_orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_no VARCHAR(50) UNIQUE NOT NULL,
  customer_id UUID REFERENCES customers(id),
  customer_po_no VARCHAR(100),
  order_date DATE NOT NULL,
  delivery_date DATE,
  total_amount DECIMAL(12, 4) DEFAULT 0,
  discount_amount DECIMAL(12, 4) DEFAULT 0,
  tax_amount DECIMAL(12, 4) DEFAULT 0,
  final_amount DECIMAL(12, 4) DEFAULT 0,
  status VARCHAR(20) DEFAULT 'DRAFT',
  salesperson_id UUID REFERENCES users(id),
  approved_by UUID,
  approved_at TIMESTAMP,
  remark TEXT,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_by UUID
);

CREATE INDEX idx_sales_orders_order_no ON sales_orders(order_no);
CREATE INDEX idx_sales_orders_customer ON sales_orders(customer_id);
CREATE INDEX idx_sales_orders_status ON sales_orders(status);
CREATE INDEX idx_sales_orders_order_date ON sales_orders(order_date);

-- 分区表（按月份分区）
CREATE TABLE sales_orders_partitioned (
  LIKE sales_orders INCLUDING DEFAULTS INCLUDING CONSTRAINTS
) PARTITION BY RANGE (order_date);

-- 创建分区示例
CREATE TABLE sales_orders_2024_01 PARTITION OF sales_orders_partitioned
  FOR VALUES FROM ('2024-01-01') TO ('2024-02-01');
```

#### 3.2.4 库存表（inventory）

```sql
CREATE TABLE inventory (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  warehouse_id UUID REFERENCES warehouses(id) NOT NULL,
  product_id UUID REFERENCES products(id) NOT NULL,
  batch_id UUID REFERENCES batches(id) NOT NULL,
  quantity DECIMAL(12, 4) DEFAULT 0,
  available_quantity DECIMAL(12, 4) DEFAULT 0,
  locked_quantity DECIMAL(12, 4) DEFAULT 0,
  unit_cost DECIMAL(12, 4),
  last_movement_at TIMESTAMP,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_by UUID,
  UNIQUE(warehouse_id, product_id, batch_id)
);

CREATE INDEX idx_inventory_warehouse ON inventory(warehouse_id);
CREATE INDEX idx_inventory_product ON inventory(product_id);
CREATE INDEX idx_inventory_batch ON inventory(batch_id);
```

#### 3.2.5 批次表（batches）

```sql
CREATE TABLE batches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_no VARCHAR(50) UNIQUE NOT NULL,
  vat_no VARCHAR(50),
  product_id UUID REFERENCES products(id) NOT NULL,
  color_formula_id UUID REFERENCES color_formulas(id),
  quantity DECIMAL(12, 4) NOT NULL,
  production_date DATE,
  expiry_date DATE,
  lab_values JSONB,
  quality_status VARCHAR(20) DEFAULT 'PENDING',
  inspection_report JSONB,
  remark TEXT,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by UUID,
  updated_by UUID
);

CREATE INDEX idx_batches_batch_no ON batches(batch_no);
CREATE INDEX idx_batches_product ON batches(product_id);
CREATE INDEX idx_batches_quality_status ON batches(quality_status);
```

---

## 4. API 实现规范

### 4.1 REST API 设计规范

#### 4.1.1 URL 结构

```
/api/v1/{module}/{resource}/{id}/{action}
```

示例：
- `GET /api/v1/sales/orders` - 获取销售订单列表
- `GET /api/v1/sales/orders/123e4567-e89b-12d3-a456-426614174000` - 获取订单详情
- `POST /api/v1/sales/orders` - 创建订单
- `PUT /api/v1/sales/orders/:id` - 更新订单
- `POST /api/v1/sales/orders/:id/approve` - 审批订单
- `POST /api/v1/sales/orders/:id/ship` - 发货

#### 4.1.2 请求/响应格式

**请求**：
```typescript
// GET /api/v1/sales/orders?page=1&limit=20&status=APPROVED
{
  "page": 1,
  "limit": 20,
  "sortBy": "createdAt",
  "sortOrder": "DESC",
  "filters": {
    "status": "APPROVED",
    "customerId": "xxx"
  }
}
```

**响应**：
```typescript
// 成功响应
{
  "success": true,
  "data": {
    "items": [...],
    "total": 100,
    "page": 1,
    "limit": 20
  },
  "message": "获取成功",
  "timestamp": "2026-06-06T12:00:00Z"
}

// 分页列表响应
{
  "success": true,
  "data": {
    "items": [...],
    "total": 150,
    "page": 1,
    "limit": 20,
    "totalPages": 8
  }
}

// 错误响应
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "输入数据验证失败",
    "details": [
      {
        "field": "email",
        "message": "邮箱格式不正确"
      }
    ]
  },
  "timestamp": "2026-06-06T12:00:00Z"
}
```

#### 4.1.3 HTTP 状态码

| 状态码 | 说明 | 场景 |
|--------|------|------|
| 200 | OK | GET 请求成功 |
| 201 | Created | POST 创建成功 |
| 204 | No Content | DELETE 删除成功 |
| 400 | Bad Request | 请求参数错误 |
| 401 | Unauthorized | 未登录或 Token 失效 |
| 403 | Forbidden | 无权限 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 资源冲突（如重复创建） |
| 422 | Unprocessable Entity | 验证失败 |
| 429 | Too Many Requests | 请求超限 |
| 500 | Internal Server Error | 服务器内部错误 |

### 4.2 NestJS 控制器实现规范

```typescript
@Controller('sales/orders')
@ApiTags('销售订单')
@UseGuards(JwtAuthGuard, RolesGuard)
export class SalesOrderController {
  constructor(private readonly salesOrderService: SalesOrderService) {}

  @Get()
  @ApiOperation({ summary: '获取销售订单列表' })
  @Roles(Role.SALES, Role.ADMIN)
  async findAll(
    @Query() query: FindSalesOrdersDto,
    @CurrentUser() user: User,
  ): Promise<PaginatedResult<SalesOrder>> {
    return this.salesOrderService.findAll(query, user);
  }

  @Get(':id')
  @ApiOperation({ summary: '获取销售订单详情' })
  async findOne(@Param('id') id: string): Promise<SalesOrder> {
    return this.salesOrderService.findOne(id);
  }

  @Post()
  @ApiOperation({ summary: '创建销售订单' })
  async create(
    @Body() createDto: CreateSalesOrderDto,
    @CurrentUser() user: User,
  ): Promise<SalesOrder> {
    return this.salesOrderService.create(createDto, user);
  }

  @Put(':id')
  @ApiOperation({ summary: '更新销售订单' })
  async update(
    @Param('id') id: string,
    @Body() updateDto: UpdateSalesOrderDto,
    @CurrentUser() user: User,
  ): Promise<SalesOrder> {
    return this.salesOrderService.update(id, updateDto, user);
  }

  @Post(':id/approve')
  @ApiOperation({ summary: '审批销售订单' })
  @Roles(Role.SALES_MANAGER, Role.ADMIN)
  async approve(
    @Param('id') id: string,
    @Body() approveDto: ApproveSalesOrderDto,
    @CurrentUser() user: User,
  ): Promise<SalesOrder> {
    return this.salesOrderService.approve(id, approveDto, user);
  }
}
```

---

## 5. 事件驱动架构实现

### 5.1 事件类型定义

```typescript
// 系统事件枚举
export enum DomainEventType {
  USER_CREATED = 'user.created',
  USER_UPDATED = 'user.updated',
  
  SALES_ORDER_CREATED = 'sales_order.created',
  SALES_ORDER_APPROVED = 'sales_order.approved',
  SALES_ORDER_SHIPPED = 'sales_order.shipped',
  SALES_ORDER_COMPLETED = 'sales_order.completed',
  
  PURCHASE_ORDER_CREATED = 'purchase_order.created',
  PURCHASE_ORDER_RECEIVED = 'purchase_order.received',
  
  INVENTORY_INBOUND = 'inventory.inbound',
  INVENTORY_OUTBOUND = 'inventory.outbound',
  INVENTORY_ADJUSTED = 'inventory.adjusted',
  
  BATCH_CREATED = 'batch.created',
  BATCH_INSPECTED = 'batch.inspected',
  
  COLOR_FORMULA_APPROVED = 'color_formula.approved',
  
  FINANCIAL_TRANSACTION_CREATED = 'financial.transaction.created',
}

// 基础事件接口
export interface DomainEvent {
  type: DomainEventType;
  aggregateId: string;
  aggregateType: string;
  payload: any;
  timestamp: Date;
  userId?: string;
}
```

### 5.2 事件发布与订阅实现

```typescript
// 事件发布器
@Injectable()
export class EventPublisherService {
  constructor(private eventEmitter: EventEmitter2) {}

  async publish<T extends DomainEvent>(event: T): Promise<void> {
    // 记录事件日志
    await this.logEvent(event);
    
    // 发送事件
    this.eventEmitter.emit(event.type, event);
    
    // 发送到 Redis 消息队列（可选）
    // await this.redisService.publish('events', JSON.stringify(event));
  }

  private async logEvent(event: DomainEvent): Promise<void> {
    // 保存到事件日志表
    // await this.eventLogRepository.save({ ...event });
  }
}

// 事件监听器
@Injectable()
export class SalesOrderListener {
  @OnEvent(DomainEventType.SALES_ORDER_CREATED)
  async handleOrderCreated(event: DomainEvent): Promise<void> {
    console.log('订单创建事件:', event);
    
    // 触发库存锁定
    // await this.inventoryService.lockInventory(event.payload.orderId);
    
    // 发送通知
    // await this.notificationService.sendToSalesManagers(event);
  }

  @OnEvent(DomainEventType.SALES_ORDER_APPROVED)
  async handleOrderApproved(event: DomainEvent): Promise<void> {
    console.log('订单审批通过事件:', event);
    
    // 创建生产工单
    // await this.productionService.createWorkOrder(event.payload.orderId);
    
    // 记录财务交易
    // await this.financeService.createReceivable(event.payload.orderId);
  }
}
```

---

## 6. 性能优化方案

### 6.1 数据库优化

1. **索引优化**
   - 在经常查询的字段上建立索引
   - 复合索引优化多字段查询
   - 定期分析索引使用情况

2. **查询优化**
   - 避免 SELECT *，只查询需要的字段
   - 使用 JOIN 替代子查询
   - 批量操作替代单条操作

3. **分区表**
   - 大表（如销售订单、库存流水）按时间分区
   - 提高查询效率，便于归档

### 6.2 Redis 缓存策略

**缓存层级**：
```
L1: 本地内存缓存（LRU）- 高频配置数据
L2: Redis 缓存 - 共享数据
L3: 数据库 - 持久化数据
```

**缓存实现**：
```typescript
@Injectable()
export class CacheService {
  constructor(@InjectRedis() private redis: Redis) {}

  async getOrSet<T>(
    key: string,
    factory: () => Promise<T>,
    ttl = 3600,
  ): Promise<T> {
    const cached = await this.redis.get(key);
    if (cached) {
      return JSON.parse(cached);
    }
    
    const value = await factory();
    await this.redis.setex(key, ttl, JSON.stringify(value));
    return value;
  }

  async invalidatePattern(pattern: string): Promise<void> {
    const keys = await this.redis.keys(pattern);
    if (keys.length > 0) {
      await this.redis.del(...keys);
    }
  }
}

// 使用示例
async getProduct(id: string): Promise<Product> {
  return this.cacheService.getOrSet(
    `product:${id}`,
    () => this.productRepository.findOneBy({ id }),
    1800, // 30分钟
  );
}
```

### 6.3 分页查询优化

```typescript
// 高效分页实现
async findAll(page: number, limit: number, filters: any): Promise<PaginatedResult<any>> {
  const queryBuilder = this.repository.createQueryBuilder('entity');
  
  // 应用过滤条件
  if (filters.status) {
    queryBuilder.andWhere('entity.status = :status', { status: filters.status });
  }
  
  // 排序
  queryBuilder.orderBy('entity.created_at', 'DESC');
  
  // 获取总数（使用 count 查询）
  const total = await queryBuilder.getCount();
  
  // 分页
  const items = await queryBuilder
    .skip((page - 1) * limit)
    .take(limit)
    .getMany();
  
  return {
    items,
    total,
    page,
    limit,
    totalPages: Math.ceil(total / limit),
  };
}
```

---

## 7. 安全实现方案

### 7.1 认证与授权

**JWT 认证流程**：
1. 用户登录 → 返回 Access Token + Refresh Token
2. 请求 API → 携带 Access Token 在 Header
3. Token 过期 → 使用 Refresh Token 获取新的 Access Token
4. Refresh Token 过期 → 重新登录

**RBAC 实现**：
```typescript
// 角色装饰器
@SetMetadata('roles', [Role.ADMIN, Role.SALES_MANAGER])

// 角色守卫
@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const requiredRoles = this.reflector.getAllAndOverride<Role[]>('roles', [
      context.getHandler(),
      context.getClass(),
    ]);
    
    if (!requiredRoles) {
      return true;
    }
    
    const { user } = context.switchToHttp().getRequest();
    return requiredRoles.some((role) => user.roles?.includes(role));
  }
}
```

### 7.2 数据安全

1. **敏感数据加密**
   ```typescript
   // 密码加密
   const hashedPassword = await bcrypt.hash(password, 10);
   
   // 敏感字段加密存储
   const encryptedPhone = encrypt(phone);
   ```

2. **SQL 注入防护**
   - 使用 TypeORM 查询构建器
   - 避免字符串拼接 SQL

3. **XSS 防护**
   - 用户输入转义
   - 使用安全的模板渲染

4. **审计日志**
   ```typescript
   @Injectable()
   export class AuditLogService {
     async log(
       action: string,
       entityType: string,
       entityId?: string,
       before?: any,
       after?: any,
     ): Promise<void> {
       await this.auditLogRepository.create({
         action,
         entityType,
         entityId,
         before,
         after,
         userId: this.currentUser?.id,
         ipAddress: this.currentIp,
         timestamp: new Date(),
       });
     }
   }
   ```

---

## 8. 测试实现方案

### 8.1 测试金字塔

```
        /\
       /E2E\    <-- 端到端测试（Playwright）
      /------\
     / 集成测试 \ <-- API 集成测试（Jest + Supertest）
    /----------\
   /  单元测试   \ <-- 单元测试（Jest）
  /--------------\
```

### 8.2 单元测试示例

```typescript
describe('UserService', () => {
  let service: UserService;
  let repository: MockType<Repository<User>>;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        UserService,
        {
          provide: getRepositoryToken(User),
          useFactory: repositoryMockFactory,
        },
      ],
    }).compile();

    service = module.get<UserService>(UserService);
    repository = module.get(getRepositoryToken(User));
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  describe('create', () => {
    it('should create a user', async () => {
      const createUserDto = {
        username: 'testuser',
        email: 'test@example.com',
        password: 'password123',
        name: 'Test User',
      };

      const expectedUser = { id: '1', ...createUserDto };
      repository.create.mockReturnValue(expectedUser);
      repository.save.mockResolvedValue(expectedUser);

      const result = await service.create(createUserDto);

      expect(result).toEqual(expectedUser);
      expect(repository.create).toHaveBeenCalled();
      expect(repository.save).toHaveBeenCalled();
    });
  });
});
```

### 8.3 E2E 测试示例

```typescript
describe('Auth (e2e)', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
  });

  it('/api/v1/auth/login (POST)', async () => {
    const loginDto = {
      username: 'admin',
      password: 'admin123',
    };

    const response = await request(app.getHttpServer())
      .post('/api/v1/auth/login')
      .send(loginDto)
      .expect(201);

    expect(response.body).toHaveProperty('accessToken');
    expect(response.body).toHaveProperty('refreshToken');
  });

  afterAll(async () => {
    await app.close();
  });
});
```

---

## 附录

### A. 部署清单

- [ ] 代码库准备完成
- [ ] CI/CD 流水线配置
- [ ] Docker 镜像构建
- [ ] Kubernetes 部署文件
- [ ] 数据库初始化脚本
- [ ] 监控告警配置
- [ ] 备份策略配置
- [ ] 生产环境配置检查
- [ ] 安全扫描通过
- [ ] 性能测试通过

### B. 运维检查清单

- [ ] 数据库备份正常（每日/每周/每月）
- [ ] 监控指标正常（CPU/内存/磁盘）
- [ ] 日志收集正常（ELK）
- [ ] 告警通知正常（邮件/短信）
- [ ] 安全补丁及时更新
- [ ] 性能定期优化
- [ ] 容灾演练定期执行
